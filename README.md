# Student Record Management System (SRMS)

📌 Overview

This project is a **CLI-based Student Record Management System** built using **Java**. It provides a simple and interactive menu-driven interface to perform CRUD operations on student records.

The system uses **OOP concepts** (classes, objects, encapsulation) and stores data in an `ArrayList`. It is ideal for beginners learning Java, file structure, and basic data handling.

---

🎯 Features

* Add new student records
* View all student details
* Search student by ID
* Update student information
* Delete student record
* Menu-driven interface using loops
* Clean and structured Java code with PSVM

---

## 🛠 Tools Used

* **Java (JDK 8+)**
* **VS Code** or **IntelliJ IDEA Community Edition**
* **Terminal / Command Prompt**

---

## 📁 Project Structure

```
StudentRecordManagement.java
```

Contains:

* `Student` class (ID, Name, Course, Grade)
* `StudentRecordManagement` class (main program)
* CRUD operation methods
* Menu loop inside `public static void main`

---

## ▶️ How to Run the Program

1. Install **JDK 8 or later**.
2. Open terminal in the project folder.
3. Compile the file:

```
javac StudentRecordManagement.java
```

4. Run the program:

```
java StudentRecordManagement
```

---

## 📌 Usage

Follow the on-screen menu options:

* Enter student details
* View all records
* Search, update, or delete using student ID
* Exit when done

---

